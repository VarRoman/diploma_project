\documentclass[ukrainian, utf8, columnxxvi]{eskdgraph}

\input{common/preamble-graph}

\stampDocName{\SStranscript}
\stampDocId{\SSid}{\SSh}

\begin{document}

\begin{ESKDdrawing}
\centering
\begin{tikzpicture}[
  font=\small,
  root/.style={rectangle, draw=black, line width=0.6pt, fill=orange!22,
               text width=10.5cm, minimum height=1cm, align=center, font=\bfseries},
  layer/.style={rectangle, draw=black, line width=0.6pt, fill=blue!18,
               text width=3.7cm, minimum height=0.95cm, align=center, font=\small\bfseries},
  comp/.style={rectangle, draw=black, fill=blue!6,
               text width=3.7cm, minimum height=1.05cm, align=center, inner sep=3pt},
  link/.style={thick, draw=black},
]

  % --- корінь ---
  \node[root] (root) at (5.7,0)
    {Програмний комплекс просторового аналізу гри у волейбол};

  % --- координуючий модуль ---
  \node[layer, fill=orange!12, text width=6cm] (orch) at (5.7,-1.6)
    {Оркестратор конвеєра (run\_pipeline.sh)};

  % --- рівні (підсистеми верхнього рівня) ---
  \node[layer] (L1) at (0,-3.4)   {Підсистема введення та ресурсів};
  \node[layer] (L2) at (5.7,-3.4) {Підсистема обробки даних};
  \node[layer] (L3) at (11.4,-3.4){Підсистема подання результатів};

  % --- компоненти підсистеми введення ---
  \node[comp, below=0.55cm of L1] (a1) {Завантажувач відеопотоку};
  \node[comp, below=0.45cm of a1] (a2) {Сховище вагової моделі YOLO26};
  \node[comp, below=0.45cm of a2] (a3) {Параметри калібрування камери};
  \node[comp, below=0.45cm of a3] (a4) {Файл конфігурації запуску};

  % --- компоненти підсистеми обробки ---
  \node[comp, below=0.55cm of L2] (b1) {Модуль детекції \\ (YOLO26)};
  \node[comp, below=0.45cm of b1] (b2) {Модуль геометричного відновлення \\ (PnP, зворотне проєктування)};
  \node[comp, below=0.45cm of b2] (b3) {Модуль трекінгу \\ (IMM/UKF, 9D-стан)};
  \node[comp, below=0.45cm of b3] (b4) {Модуль обчислення статистики};

  % --- компоненти підсистеми подання ---
  \node[comp, below=0.55cm of L3] (c1) {Модуль візуалізації (відео-оверлей)};
  \node[comp, below=0.45cm of c1] (c2) {Експорт 3D-траєкторій};
  \node[comp, below=0.45cm of c2] (c3) {Формування звіту статистики};

  % --- зв'язки корінь -> оркестратор -> рівні ---
  \draw[link] (root.south) -- (orch.north);
  \draw[link] (orch.south) -- ++(0,-0.45) -| (L1.north);
  \draw[link] (orch.south) -- ++(0,-0.45) -| (L3.north);
  \draw[link] (orch.south) -- (L2.north);

  % --- зв'язки рівень -> компоненти (вертикальний ланцюг "входить до складу") ---
  \draw[link] (L1.south) -- (a1.north);
  \draw[link] (a1.south) -- (a2.north);
  \draw[link] (a2.south) -- (a3.north);
  \draw[link] (a3.south) -- (a4.north);

  \draw[link] (L2.south) -- (b1.north);
  \draw[link] (b1.south) -- (b2.north);
  \draw[link] (b2.south) -- (b3.north);
  \draw[link] (b3.south) -- (b4.north);

  \draw[link] (L3.south) -- (c1.north);
  \draw[link] (c1.south) -- (c2.north);
  \draw[link] (c2.south) -- (c3.north);

\end{tikzpicture}
\end{ESKDdrawing}

\end{document}
